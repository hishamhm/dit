FILES
name suffix .toml
name suffix Cargo.lock
RULES

script diff.lua

include gitconflict.dithlinc

context # `$ dim
/context

context " " alt
/context

context [[ ]] brightalt brightalt brightdiff
   context ' ' diff
   /context
/context

context `^[ ] diff diff brightdiff
   context ' ' diff
   /context
/context

rule = bright
rule { brightalt
rule , bright
rule } brightalt
rule [ brightalt
rule ] brightalt
rule . brightdiff

rule true alt
rule false alt